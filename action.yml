# action.yml
name: 'centos8'
description: 'build in centos8'
inputs:
  shell_main:
    description: 'main'
    required: true
    default: 'action.sh'
runs:
  using: 'docker'
  image: 'Dockerfile'
  entrypoint: '/main.sh'
  pre-entrypoint: '/setup.sh'
  post-entrypoint: '/cleanup.sh'

